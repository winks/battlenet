(ns battlenet.test.core
  (:use [battlenet.core])
  (:use [battlenet.defs])
  (:use [battlenet.model])
  (:use [battlenet.tools])
  (:use [battlenet.network])
  (:use [battlenet.test.mock])
  (:use [clojure.test]))